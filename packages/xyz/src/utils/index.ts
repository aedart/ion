/**
 * Simple multiplier utility in a nested directory
 *
 * @param {number} a
 * @param {number} b
 *
 * @returns {number}
 */
export function multiply(a: number, b: number): number
{
    return a * b;
}
